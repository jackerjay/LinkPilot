# @linkpilot/config

TypeScript DSL for authoring `linkpilot.config.ts`. Compiles to the same JSON
schema the GUI edits. Lands in **v0.2+** (see plan §决策 3).

This directory is intentionally empty until then.
